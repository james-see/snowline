# Controls

Default map: [GAME_DESIGN.md](GAME_DESIGN.md).

Input is data-driven via `Input.rebind`. Gamepad: standard mapping (stick steer, A jump, B brake, LB/RT boost, LT lean, Start pause).

Settings toggles: landing assist, reduced camera shake.
