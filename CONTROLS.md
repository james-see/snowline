# Controls

See [GAME_DESIGN.md](GAME_DESIGN.md) for the default map.

Input is data-driven via `Input.rebind`. Gamepad uses the standard mapping (stick steer, A jump, B brake, LB/RT boost, LT lean, Start pause).

Landing assist and reduced camera shake are toggles in Settings.
